export interface ProductColor {
    id: string;
    name: string;
    hex: string;
    className: string;
    borderColorClass: string;
    ringColorClass: string;
    imageSrc: string;
}

export interface NavItem {
    label: string;
    href: string;
}